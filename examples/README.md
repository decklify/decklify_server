# Examples

This folder contains a complete first config that you can try out.

It includes almost all possible ways to customize and configure the deck (as of now).

To use it, simply place the `config`, `assets` and `macros` folders in this location : `C:\Users\<YOUR_USER>\AppData\Local\Decklify\Decklify`.
